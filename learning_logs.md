# RM_RLogs
## 1.19
### 笔记：
1. gym.register()
### 进度：
1. 配置了isaaclab环境
2. 配置了一个manager_based project用于训练，但有点复杂而且与文档教程不兼容，故又配置了一个direct workflow的project来熟悉和学习
3. 在看isaaclab文档，目前看到workthrough部分，但是前面还不能完全理解，后面要边做边对照
### 计划：
1. *根据workthrough跑通jetbot的训练，在这个过程中搞清楚direct类型的工程整个的结构（以搞懂结构为主，不需要全部跑一遍）*
2. *尝试跑通manager_based下的jetbot的训练，可以对照云深处的开源*
3. *问一下AI4code的技巧，尝试解决下这个代码间跳转的问题*
### 碎碎念：
1. vscode的项目有点复杂，然后代码之间的跳转很烂，好像整个文件没有链接起来一样，copilot看起来也笨笨的。感觉快速理解整个工程项目的能力还有欠缺。要考虑拿出一些时间“磨刀”
2. 每隔一个小时要出去接一瓶水，要及时回消息（对了，给自己买个水杯）

## 1.20
### 笔记：
1. 代码跳转的问题大致上定位了，是pylance看不到isaaclab的文件结构，可能需要重新配置一个python3.10版本的虚拟环境，然后安装二进制文件的isaacsim而非像官方教程这样只下载一个包，决定先把昨天的计划进度完成再说
2. 在尝试教程的过程遇到以下问题：
   * num_envs为100时直接给kill了，改成4才搞好，不知道为什么跑云深处和小蜘蛛的时候都没有这个问题，**之后研究一下**
   * 教程的修改逻辑是基于skrl而非rsl_rl的，因此要做些魔改，**之后具体学下rsl_rl的接口规范**
3. 尝试调整训练的迭代数和环境数，发现环境数对训练速度的影响是对数级的，不过基本不太影响训练时间，因此还是要尽可能多些
### 进度：
1. 尝试修代码跳转问题，无果，但大概直到怎么解决，跑通之后修
2. 跑通了jetbot的初始训练程序，打算进一步加箭头，但还没调好
### 计划：
1. direct_rl上跑完jetbot
2. 移植到manager_based上
### 碎碎念：
1.跑通了教程的小车，很开心，基于这个小车改成平衡车，就算跑通了（）

## 1.21
### 笔记：
1. rsl_rl代码规范 需区分张量和向量
2. 奖励为加和形式时，收敛过程振荡很大，前期有一个下降段，加大环境数量会有改善；训出来的结果有明显偏航角超调。
3. 将奖励改为前进速度*cos指数后，收敛快且平稳，yaw角度的回正丝滑无超调
### 进度：
1. 跑完了direct的jetbot库
2. 开始将direct移植到manager_based

## 1.22
### 笔记：
1. 下载了新项目后要跑一下源代码的setup文件，这个还能出错也太唐氏了
### 进度：
1. 配好了新主机（但是蓝工帮了大部分忙）和RL环境
2. 把在笔记本上跑通了的程序发到了主机上跑通了
3. 继续移植manager_based

## 1.23
### 笔记：
### 进度：
1. 代码跳转解决了，把isaaclab源码和自己的工程放一起，然后代码修复一下就好了，具体怎么修复的还有待观察